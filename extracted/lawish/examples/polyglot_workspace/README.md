# Example: polyglot_workspace

A cargo-less workspace (Python + JavaScript). lawish observes structure via the
file=module law and the cross-language extractor.

```sh
cargo run --bin lawish -- --wish "a module calc" --no-color examples/polyglot_workspace
```
